<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>REsult 20222</title>
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@4.6.1/dist/css/bootstrap.min.css">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/4.7.0/css/font-awesome.min.css">
    <link rel="stylesheet" href="\css\index.css">
    <link rel="stylesheet" href="\css\admin.css">
</head>
<body>
    <div class="site-wrapper">
        <div class="header-wrap">
            <h1 class="site-hdr">Result Management System</h1>
            <form class="logout-form" action="/logout" method="get">
                <button class="logout-btn btn btn-warning">Logout</button>
               </form>
        </div>
        <div class="main-content">
        <div class="table-wrapper col-lg-8 col-md-8 col-sm-12">
            <div class="table-hdr">
                <h4>Total Number of student</h4>
                 
                <a href="/add"  class="btn btn-primary">Add Records</a>
            </div>
            <br>
           <table class="stud-table">
               <tr>
                   <th>Roll No</th>
                   <th>Name</th>
                   <th>DOB</th>
                   <th>Score</th>
                   <th>Actions</th>
               </tr>
               <%if(locals.item.length!=0){%>
                <% item.forEach(function(stud){%>
                    <tr>
                        <td><%=stud.dataValues.id %></td>
                        <td><%=stud.dataValues.name %></td>
                        <% var date = new Date(stud.dataValues.dob);
                           year  =  date.getFullYear()
                           month  =  date.getMonth()+1;
                           dt  =  date.getDate();
                           %>

                        <td><%=dt+"-"+month+"-"+year %></td>
                        <td><%=stud.dataValues.score %></td>
                        <td class="action-area">
                            <form action="/edit" method="post">
                              <input type="hidden" name="id" value="<%=stud.dataValues.id %>">
                              <input type="hidden" name="name" value="<%=stud.dataValues.name%>">
                              <input type="hidden" name="dob" value="<%=stud.dataValues.dob %>">
                              <input type="hidden" name="score" value="<%=stud.dataValues.score %>">
                              <button type="submit"><i class='fa fa-pencil' style='font-size:24px;color:rgb(23, 129, 179)'></i></button>
                            </form>
                            <form action="/delete" method="post">
                             <input type="hidden" name="ID" value="<%=stud.dataValues.id %>">
                             <button type="submit"><i class='fa fa-close' style='font-size:24px;color:red'></i></button>
                            </form>
                        </td>
                    </tr>



               <%})%>
           <% }%>
               
           </table> 
         </div>   
        </div>
    </div>
</body>
</html>